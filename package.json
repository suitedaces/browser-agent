{
  "name": "taskhomie",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "dev:extension": "pnpm --filter @taskhomie/extension dev",
    "dev:server": "pnpm --filter @taskhomie/server dev",
    "build:extension": "pnpm --filter @taskhomie/extension build",
    "build:server": "pnpm --filter @taskhomie/server build"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
